

const INITIAL_STATE = {}
console.log(INITIAL_STATE.arrayPokemons);
const nomeReducer1 = (state = INITIAL_STATE, action) => {
 switch(action.type) {
   default:
    return state;
 }
}
export default nomeReducer1;
